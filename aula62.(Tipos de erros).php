<?php

var_dump(E_ALL); //Todos os erros
var_dump(E_STRICT); //Noticia em tempo de execução, php pode sugerir alterações
var_dump(E_NOTICE); //Encontrou algo que pode ser um erro
var_dump(E_ERROR); //Erros fatais em tempo de execução
var_dump(E_WARNING); // Avisos em tempo de execução (erros não fatais)